{
    "private": true,
    "scripts": {
        "dev:app": "pnpm -C app run dev",
        "tauri-dev": "pnpm -C app run tauri dev",
        "tauri-build": "pnpm -C app run tauri build",
        "dev:web": "npm --prefix web run dev"
    }
}
