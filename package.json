{
    "private": true,
    "workspaces": [
        "app",
        "web"
    ],
    "scripts": {
        "dev:app": "npm --prefix app run dev",
        "tauri-dev": "npm --prefix app run tauri dev",
        "dev:web": "npm --prefix web run dev"
    }
}